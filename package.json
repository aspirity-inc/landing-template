{
  "name": "landing-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild": "rm -rf public",
    "build": "parcel build --dist-dir public src/*.html",
    "preserve": "rm -rf dist",
    "serve": "parcel --port 8080 src/*.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-less": "^2.8.0",
    "parcel": "2.8.0"
  }
}
