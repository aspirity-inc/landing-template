{
  "name": "landing-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf public",
    "build": "parcel build --out-dir public src/*.html",
    "preserve": "rm -rf dist",
    "serve": "parcel serve --port 8080 src/*.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "1.12.3"
  }
}
